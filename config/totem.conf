# enable zmk studio for realtime keymap updates
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n

# mouse move/scroll/click (&mmv, &msc, &mkp) — off by default; required for pointing.h behaviors
CONFIG_ZMK_POINTING=y